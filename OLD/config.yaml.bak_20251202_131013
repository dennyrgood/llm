# Set the default model to use when you start aichat.
# CRITICAL FIX: Include client prefix 'ollama:' and use quotes.
# Setting the default to the robust 'alfred' model.
model: "ollama:Qwen2.5-coder:1.5b"

# The clients block defines how aichat connects to different providers.
clients:
  - type: openai-compatible
    name: ollama
    
    # CRITICAL: This base URL MUST include the /v1 path for aichat to work with Ollama.
    # NOTE: You are using HTTPS, ensure your certificate is correctly configured.
    api_base: https://ollama.ldmathes.cc/v1
    
    # Ollama does not use an API key, but aichat often requires a placeholder.
    api_key: "sk-ollama-placeholder"
    
    # List the specific models available on your Ollama server.
    # I have ordered them logically for easier reading/management.
    models:
      # --- Cloud Models ---
      - name: glm-4.6:cloud
        display_name: GLM 4.6 (Cloud)

      # NOTE: These Cloud models are highly recommended and are often requested
      - name: minimax-m2:cloud
        display_name: MiniMax M2 (Cloud)
        
      - name: kimi-k2-thinking:cloud
        display_name: Kimi K2 (Cloud)

      # --- General Purpose / Reasoning Models ---
      - name: alfred:latest
        display_name: Alfred (Falcon 40B Base)
        
      - name: notus:latest
        display_name: Notus (Mistral Fine-tune)
        
      - name: mistral:latest
        display_name: Mistral 7B

      - name: deepseek-r1:latest
        display_name: DeepSeek R1 (Reasoning)
        
      # --- Code Models ---
      - name: firefunction-v2:latest
        display_name: Firefunction V2 (Code/Agent)
        
      - name: qwen3-coder:latest
        display_name: Qwen3 Coder

      # --- Lightweight Models ---
      - name: gemma3:4b
        display_name: Gemma 3 (4B)
        
      - name: gemma3:1b
        display_name: Gemma 3 (1B)

      - name: gpt-oss:120b-cloud
        display_name: gpt oss 120b (cloud)


      # --- Local Models (Missing) ---
      - name: deepseek-v3.1:671b-cloud
        display_name: Deepseek V3.1 (671b-cloud)

      - name: llava:13b
        display_name: Llava (13b)

      - name: qwen3-coder:480b-cloud
        display_name: Qwen3 Coder (480b-cloud)

      - name: qwen2.5-coder:1.5b
        display_name: Qwen2.5 Coder (1.5b)

      # --- Local Models (Missing) ---
      - name: gemma2:2b
        display_name: Gemma2 (2b)

      - name: phi3:mini
        display_name: Phi3 (mini)

      - name: qwen2.5:1.5b
        display_name: Qwen2.5 (1.5b)

      - name: tinyllama:latest
        display_name: Tinyllama

      # --- Local Models (Missing) ---
      - name: qwen2.5:7b
        display_name: Qwen2.5 (7b)

      - name: qwen2.5:14b
        display_name: Qwen2.5 (14b)

      - name: phi3:mini
        display_name: Phi 3 Mini
